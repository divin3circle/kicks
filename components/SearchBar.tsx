import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SearchBar = () => {
  return (
    <View className="bg-white flex-1">
      <Text className="text-black">SearchBar</Text>
    </View>
  );
};

export default SearchBar;

const styles = StyleSheet.create({});
